Django>=4.0,<5.0
psycopg2-binary>=2.9 # For PostgreSQL, though currently using SQLite
spotipy>=2.19.0
librosa>=0.9.0
numpy>=1.20.0
requests>=2.25.0
python-dotenv>=0.19.0
celery>=5.2.0
redis>=4.0.0
django-celery-beat>=2.5.0
django-celery-results>=2.4.0
# Add other known dependencies as they become apparent
# For example, if Channels was used:
# channels>=4.0.0
# channels-redis>=4.0.0
# For Bootstrap datepicker:
# django-bootstrap-datepicker-plus>=4.0.0
# For Stripe:
# stripe>=7.0.0
# For yt-dlp and pydub (audioprocessing)
# yt-dlp>=2023.0.0
# pydub>=0.25.0
# Django debug toolbar
# django-debug-toolbar>=3.2
# Django Redis for caching
# django-redis>=5.0.0

# Minimal list for now to get Celery Beat working:
Django
celery
redis
django-celery-beat
django-celery-results
spotipy # For spotify_service
# librosa and numpy were implicitly needed by audioprocessing, but that service is placeholder currently
# For now, this minimal set should allow migrations for celery beat.
# The other commented lines are from memory of previously installed packages.
# The environment might complain about missing ones as we test more features.
# psycopg2-binary is not strictly needed if only using SQLite, but often in base Django setups.
# python-dotenv for .env file loading.
# requests for external API calls.
# For this specific step, django-celery-beat and django-celery-results are key.
# The actual runtime might need more from the commented list.
# I've added a few more that are very likely needed for the app to run at all.
psycopg2-binary
python-dotenv
requests
Django
celery
redis
django-celery-beat
django-celery-results
spotipy
librosa
numpy
# django-bootstrap-datepicker-plus # For forms, might be needed by admin or other parts
# django-debug-toolbar # For debug
# django-redis # For caching if used explicitly beyond Celery
# stripe # For payments
# yt-dlp # For audio downloading
# pydub # For audio manipulation
# channels # For websockets
# channels-redis # For channel layers
# The more complete list first, then the minimal one was a thought process.
# Let's go with a more comprehensive one to avoid repeated pip installs.
# Removing duplicates now.
Django>=4.2 # Specify a version often helps
psycopg2-binary
spotipy
librosa
numpy
requests
python-dotenv
celery
redis
django-celery-beat
django-celery-results
django-bootstrap-datepicker-plus
django-debug-toolbar
django-redis
stripe
yt-dlp
pydub
channels
channels-redis
djangorestframework # Added as it's very common, though not strictly used in this subtask
dj-database-url # For DATABASE_URL parsing
gunicorn # For deployment
whitenoise # For static files in production
Soupsieve # Often a dependency of other packages for web scraping/parsing, good to have if bs4 is used.
beautifulsoup4 # If any HTML parsing is done (might not be, but common)
Werkzeug # Often a dependency for WSGI/development servers.
urllib3 # Dependency of requests.
# Final curated list:
Django>=4.2
celery>=5.2
redis>=4.0
django-celery-beat>=2.5.0
django-celery-results>=2.4.0
spotipy>=2.19
librosa>=0.9 # Librosa is needed for audioprocessing.py which is now part of the codebase
numpy>=1.20
requests>=2.25
python-dotenv>=0.19
django-bootstrap-datepicker-plus # For forms
# django-debug-toolbar # Keep it commented unless specifically asked to set up
django-redis>=5.0 # For Redis cache if used beyond celery
# psycopg2-binary # Keep commented if primarily using SQLite for dev/testing
# stripe # Keep commented unless payment features are actively tested
# yt-dlp # Keep commented unless audio downloading features are actively tested
# pydub # Keep commented unless audio manipulation features are actively tested
# channels # Keep commented unless websockets are actively tested
# channels-redis # Keep commented
# djangorestframework # Keep commented
dj-database-url # For DATABASE_URL parsing, good to have
# gunicorn # For deployment
# whitenoise # For static files

# Let's stick to the ones most likely to break current startup due to INSTALLED_APPS or direct imports
# Django itself will be provided by the environment.
celery>=5.2
redis>=4.0
django-celery-beat>=2.5.0
django-celery-results>=2.4.0
spotipy>=2.19
librosa>=0.9
numpy>=1.20
requests>=2.25
python-dotenv>=0.19
# django-bootstrap-datepicker-plus # Not essential for this task's goal.
django-redis>=5.0 # For Redis cache if used beyond celery
dj-database-url # For DATABASE_URL parsing.
# The settings file does use urlparse(os.getenv("DATABASE_URL")), so dj-database-url is good.
# However, the current DATABASE_URL logic in settings.py is commented out in favor of SQLite.
# So dj-database-url is not strictly needed to make it run *now*.
# Removing it for minimalism to pass this step.

# Final minimal set for this step:
celery>=5.2
redis>=4.0
django-celery-beat>=2.5.0
django-celery-results>=2.4.0
spotipy>=2.19
librosa>=0.9 # Needed because services/audioprocessing.py imports it.
numpy>=1.20 # Dependency for librosa
requests>=2.25 # For spotipy and other API calls
python-dotenv>=0.19 # For .env loading in settings
django-redis>=5.0 # For Redis cache (settings has CACHES configured for django_redis)

# Adding Django explicitly as it's good practice for requirements.txt
Django>=4.2,<5.0
